## Embedding svelte inside a mdx file

The following content renders the svelte component - trak.svelte below.

import toReact from "svelte-adapter/react";
import trak from './trak.svelte';

export const baseStyle = {
  width: "100%"
};

export const Trakdata = toReact(trak, baseStyle, "div");


<Trakdata />